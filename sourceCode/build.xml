<project name="SmartCity" default="deploy" basedir=".">

    <!-- Define properties -->
    <property name="Appmanage" value="C:\tibco_5.10\tra\5.7\bin\AppManage.exe" />
    <property name="build.dir" value="${basedir}/smartCity/Deployables" />
    <property name="app.name" value="smartCity" />
<property name="project.bw.application" value="${basedir/smartCity" />
    <!-- Define targets -->
	
   

    <target name="deploy" >
        <exec executable="${Appmanage}">
            <arg line="-deploy --propFile C:\tibco_5.10\tra\5.7\bin\AppManage.tra -app ${app.name} -ear ${build.dir}/EAR/SmartCity.ear -deployconfig ${build.dir}/Config/smatcityConf.xml -user admin -pw admin -domain admin" />
        </exec>
    </target>

</project>